# vizir_charts_demo

Chart/plot demos built on top of `vizir_core`.

This crate is intentionally separate so chart code can depend on higher-level utilities without adding dev-dependencies to `vizir_core`.

